# 🎯 ИНТЕГРАЦИЯ УЛУЧШЕННЫХ ЗАПРОСОВ В ФРОНТЕНД

**Дата обновления:** 2025-12-04  
**Статус:** ✅ **ПРОТЕСТИРОВАНО И ПОДТВЕРЖДЕНО**  
**Результаты:** Все данные (суммы, чашки, килограммы) совпадают с эталоном (10/10 записей)

---

## 📋 ПРОБЛЕМА

Ранее использовался неправильный подход:
- ❌ `COUNT(*)` для подсчета чашек (считал заказы, а не товары)
- ❌ Объединенный запрос для чашек и сумм (дублирование сумм)
- ❌ Фильтр по названиям товаров для килограммов (неточный)

---

## ✅ РЕШЕНИЕ

### 1. Раздельные запросы

**ВАЖНО:** Используйте **три отдельных запроса**:
1. Запрос для чашек (с JOIN)
2. Запрос для сумм (БЕЗ JOIN)
3. Запрос для килограммов (с JOIN и фильтром по ID групп)

### 2. Правильный подсчет чашек

Используйте `SUM(GD.Source)` вместо `COUNT(*)`:

```sql
SUM(CASE WHEN G.OWNER IN (...) THEN GD.Source ELSE NULL END) AS MonoCup
```

---

## 🔧 ПРАВИЛЬНЫЕ SQL ЗАПРОСЫ

### ЗАПРОС 1: Чашки

```sql
SELECT 
    stgp.NAME AS STORE_NAME,
    D.DAT_ AS ORDER_DATE,
    SUM(CASE WHEN G.OWNER IN ('24435','25539','21671','25546','25775','25777','25789') 
        THEN GD.Source ELSE NULL END) AS MonoCup,
    SUM(CASE WHEN G.OWNER IN ('23076','21882','25767','248882','25788') 
        THEN GD.Source ELSE NULL END) AS BlendCup,
    SUM(CASE WHEN G.OWNER IN ('24491','21385') 
        THEN GD.Source ELSE NULL END) AS CaotinaCup,
    SUM(CASE WHEN G.OWNER IN ('24435','25539','21671','25546','25775','25777','25789',
                              '23076','21882','25767','248882','25788',
                              '24491','21385') 
        THEN GD.Source ELSE NULL END) AS AllCup
FROM STORZAKAZDT D
JOIN STORZDTGDS GD ON D.ID = GD.SZID
JOIN GOODS G ON GD.GODSId = G.ID
JOIN STORGRP stgp ON D.STORGRPID = stgp.ID
WHERE D.STORGRPID IN (?, ?, ?, ?, ?)  -- ID магазинов
  AND D.CSDTKTHBID IN ('1', '2', '3', '5')
  AND D.DAT_ >= ? AND D.DAT_ <= ?
  AND NOT (
      D.comment LIKE '%мы;%' OR
      D.comment LIKE '%Мы;%' OR
      D.comment LIKE '%Тестирование%')
GROUP BY stgp.NAME, D.DAT_
ORDER BY stgp.NAME, D.DAT_
```

**Параметры:**
1. Mono группы: `['24435', '25539', '21671', '25546', '25775', '25777', '25789']`
2. Blend группы: `['23076', '21882', '25767', '248882', '25788']`
3. Caotina группы: `['24491', '21385']`
4. Все группы: `mono + blend + caotina`
5. ID магазинов: `[27, 43, 44, 46, 33]`
6. Начальная дата: `'2025-12-02'`
7. Конечная дата: `'2025-12-03'`

---

### ЗАПРОС 2: Суммы

```sql
SELECT
    stgp.NAME AS STORE_NAME,
    D.DAT_ AS ORDER_DATE,
    SUM(D.SUMMA) AS TOTAL_CASH
FROM STORZAKAZDT D
JOIN STORGRP stgp ON D.STORGRPID = stgp.ID
WHERE D.STORGRPID IN (?, ?, ?, ?, ?)  -- ID магазинов
  AND D.CSDTKTHBID IN ('1', '2', '3', '5')
  AND D.DAT_ >= ? AND D.DAT_ <= ?
  AND NOT (
      D.comment LIKE '%мы;%' OR
      D.comment LIKE '%Мы;%' OR
      D.comment LIKE '%Тестирование%')
GROUP BY stgp.NAME, D.DAT_
ORDER BY stgp.NAME, D.DAT_
```

**Параметры:**
1. ID магазинов: `[27, 43, 44, 46, 33]`
2. Начальная дата: `'2025-12-02'`
3. Конечная дата: `'2025-12-03'`

**⚠️ ВАЖНО:** БЕЗ JOIN с товарами! Это предотвращает дублирование сумм.

---

### ЗАПРОС 3: Килограммы

```sql
SELECT
    stgp.NAME AS STORE_NAME,
    D.DAT_ AS ORDER_DATE,
    SUM(GD.SOURCE) AS PACKAGES_KG
FROM STORZAKAZDT D
JOIN STORZDTGDS GD ON D.ID = GD.SZID
JOIN GOODS G ON GD.GODSId = G.ID
JOIN STORGRP stgp ON D.STORGRPID = stgp.ID
WHERE D.STORGRPID IN (?, ?, ?, ?, ?)  -- ID магазинов
  AND D.CSDTKTHBID IN ('1', '2', '3', '5')
  AND D.DAT_ >= ? AND D.DAT_ <= ?
  AND G.OWNER IN (?, ?, ?)  -- ID групп для килограммов
GROUP BY stgp.NAME, D.DAT_
ORDER BY stgp.NAME, D.DAT_
```

**Параметры:**
1. ID магазинов: `[27, 43, 44, 46, 33]`
2. Начальная дата: `'2025-12-02'`
3. Конечная дата: `'2025-12-03'`
4. ID групп: `['11077', '16279', '16276']`

**ID групп для килограммов:**
- `11077` - Coffee Blasecafe blend (250 g)
- `16279` - Coffee Blasercafe blend (1kg)
- `16276` - Coffee Blasercafe singl origin (250г)

**⚠️ ВАЖНО:** 
- Используйте фильтр по **ID групп** (`G.OWNER IN (...)`), а не по названиям товаров
- **НЕ включайте** Caotina (ID `22939`) - в Excel килограммы считаются только для Blend и Mono
- **БЕЗ фильтра по комментариям** - как в старой рабочей версии

---

## 📊 СТРУКТУРА ОТВЕТА API

После объединения трех запросов, API должен возвращать:

```json
{
  "STORE_NAME": "BatumiMall",
  "ORDER_DATE": "2025-12-02T00:00:00",
  "MonoCup": 10.0,
  "BlendCup": 39.0,
  "CaotinaCup": 6.0,
  "ALLCUP": 55.0,
  "TOTAL_CASH": 608.55,
  "PACKAGES_KG": 0.25
}
```

---

## 🎨 ОТОБРАЖЕНИЕ В UI

### Формат ячейки (компактный):

```
CCup: 6
MCup: 10
BCup: 39
📊: 55
📦 0.25кг
💰 608.55 лари
```

### Формат ячейки (детальный):

```
CCup: 6
MCup: 10
BCup: 39
📊: 55
Кг: 0.25 кг
Сумма: 608.55 лари
```

---

## ⚠️ КРИТИЧЕСКИ ВАЖНЫЕ МОМЕНТЫ

### 1. Раздельные запросы
- ❌ **НЕ объединяйте** запросы для чашек и сумм в один с JOIN
- ✅ Используйте **три отдельных запроса** и объединяйте результаты на уровне приложения

### 2. COUNT(*) vs SUM(GD.Source)
- ❌ **Неправильно:** `COUNT(*)` - считает количество заказов
- ✅ **Правильно:** `SUM(GD.Source)` - считает количество товаров

**Пример:**
```
Заказ #123:
  - 2x Espresso (Blend)
  - 1x Cappuccino (Mono)

COUNT(*): 1 чашка (один заказ) ❌
SUM(GD.Source): 3 чашки (2 + 1 товара) ✅
```

### 3. Суммы БЕЗ JOIN
- ❌ **Неправильно:** JOIN с товарами для сумм - приводит к дублированию
- ✅ **Правильно:** Суммы считаются отдельно, БЕЗ JOIN с товарами

### 4. Фильтр по ID групп для килограммов
- ❌ **Неправильно:** Фильтр по названиям товаров (`G.NAME LIKE '%250 g%' ...`)
- ✅ **Правильно:** Фильтр по ID групп (`G.OWNER IN ('11077', '16279', '16276')`)

### 5. Caotina для килограммов
- ❌ **НЕ включайте** Caotina (ID `22939`) в запрос для килограммов
- В Excel килограммы считаются только для Blend и Mono пачек

---

## 🔍 ID ГРУПП ТОВАРОВ

### Для чашек:

**Mono Cup (Single Origin):**
- `24435`, `25539`, `21671`, `25546`, `25775`, `25777`, `25789`

**Blend Cup (Смешанные сорта):**
- `23076`, `21882`, `25767`, `248882`, `25788`

**Caotina Cup (Шоколадные напитки):**
- `24491`, `21385`

### Для килограммов:

**Blend (пачки):**
- `11077` - Coffee Blasecafe blend (250 g)
- `16279` - Coffee Blasercafe blend (1kg)

**Mono (пачки):**
- `16276` - Coffee Blasercafe singl origin (250г)

---

## 📝 ЧЕКЛИСТ ИНТЕГРАЦИИ

- [ ] Заменить `COUNT(*)` на `SUM(GD.Source)` в запросе для чашек
- [ ] Разделить запросы: чашки, суммы, килограммы
- [ ] Убрать JOIN из запроса для сумм
- [ ] Использовать фильтр по ID групп для килограммов (не по названиям)
- [ ] Исключить Caotina из запроса для килограммов
- [ ] Добавить фильтр исключения тестовых заказов для чашек и сумм
- [ ] Обновить UI для отображения CCup, MCup, BCup
- [ ] Протестировать на данных за 02-03.12.2025

---

## 🧪 ТЕСТИРОВАНИЕ

### Ожидаемые значения (период 02-03.12.2025):

**Суммы:**
- BatumiMall: 608.55, 861.95
- CityMall: 698.05, 809.85
- DK Batumi: 342.00, 1014.55
- DK Paliashvili: 2874.00, 640.25
- EastPoint: 452.05, 509.00

**Килограммы:**
- BatumiMall: 0.25, 1.50
- CityMall: 1.00, 1.75
- DK Batumi: 0.75, 4.25
- DK Paliashvili: 3.00, 3.50
- EastPoint: 0.25, 0.50

**Чашки:** Проверяются по типам (CCup, MCup, BCup)

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Эталонные запросы:** `analysis/QUERY_REFERENCE.md`
- **Быстрая справка:** `analysis/FRONTEND_QUICK_REFERENCE.md`
