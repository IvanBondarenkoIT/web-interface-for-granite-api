# Flask / App
SECRET_KEY=replace_with_secret
APP_ENV=development

# Proxy API
PROXY_API_URL=http://85.114.224.45:8000
PROXY_PRIMARY_TOKEN=your_primary_token
PROXY_FALLBACK_TOKEN=your_fallback_token
PROXY_TIMEOUT=5

# Database proxy metadata (if needed for logging/debug)
APP_NAME=Firebird DB Proxy
APP_VERSION=1.0.0

# Rate limiting / misc (kept for reference)
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# CORS
ALLOWED_ORIGINS=*

