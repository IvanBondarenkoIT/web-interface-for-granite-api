# 📋 ПЛАН РЕАЛИЗАЦИИ: Страница "Остатки"

**Дата:** 2025-12-05  
**Статус:** 📝 План (готов к реализации)

---

## 🎯 ЦЕЛЬ

Добавить новую страницу "Остатки" на фронтенд с возможностью:
- Просмотра текущих остатков товаров на складе
- Отображения входящих цен
- Фильтрации по складам/группам (изначально, чтобы не загружать все данные)
- Поиска по таблице
- Отдельной кнопки загрузки остатков

---

## 📊 АНАЛИЗ ТРЕБОВАНИЙ

### Из документации STOCK_API_INTEGRATION.md:
- **SQL запрос:** Готовый запрос для получения остатков
- **Структура данных:**
  - `GROUP_NAME` - название группы товаров
  - `GROUP_ID` - ID группы товаров
  - `GOOD_ID` - ID товара
  - `GOOD_NAME` - наименование товара
  - `QUANTITY` - количество (остаток)
  - `PRICE` - цена с НДС (средняя взвешенная)
  - `TOTAL_SUM` - сумма с НДС (QUANTITY × PRICE)

### ⚠️ ВОПРОС ДЛЯ УТОЧНЕНИЯ:
В документации нет упоминания о фильтрации по складам. Возможные варианты:
1. **Фильтрация по группам товаров** (GROUP_NAME) - более вероятно
2. **Фильтрация по складам** (STORGRP) - если в БД есть связь товаров со складами
3. **Фильтрация на уровне приложения** - после получения всех данных

**Предположение:** Используем фильтрацию по группам товаров (GROUP_NAME) как изначальный фильтр.

---

## 🏗️ АРХИТЕКТУРА РЕШЕНИЯ

### 1. Backend (Python/Flask)

#### 1.1. `proxy_client.py` - Добавить метод `get_stock()`
```python
def get_stock(self, group_ids: Optional[Sequence[int]] = None) -> List[Dict[str, Any]]:
    """
    Получить остатки товаров.
    
    Args:
        group_ids: Опциональный список ID групп для фильтрации.
                  Если None, возвращаются все товары с остатками.
    
    Returns:
        Список словарей с данными об остатках.
    """
```

**SQL запрос:**
- Базовый запрос из STOCK_API_INTEGRATION.md
- Добавить опциональную фильтрацию по `G.OWNER IN (...)` если `group_ids` указан

#### 1.2. `services/stock.py` - Новый сервис для обработки остатков
```python
# Функции:
- filter_stock_by_groups(stock_data, group_ids)
- search_stock(stock_data, search_query)
- group_stock_by_category(stock_data)
- calculate_stock_summary(stock_data)
```

#### 1.3. `app.py` - Добавить маршрут `/stock`
```python
@app.route("/stock")
def stock() -> str:
    """
    Страница остатков товаров.
    
    Параметры:
    - load: "1" для загрузки данных
    - group: список ID групп для фильтрации
    - search: поисковый запрос (клиентская фильтрация)
    """
```

**Логика:**
- По умолчанию данные НЕ загружаются
- При `load=1` загружаются остатки через `client.get_stock()`
- Опциональная фильтрация по группам через параметр `group`
- Поиск выполняется на клиенте (JavaScript) для производительности

### 2. Frontend (Jinja2 Templates + Bootstrap 5)

#### 2.1. `templates/base.html` - Добавить пункт меню
```html
<li class="nav-item">
  <a class="nav-link{% if request.endpoint == 'stock' %} active{% endif %}" 
     href="{{ url_for('stock') }}">Остатки</a>
</li>
```

#### 2.2. `templates/stock_table.html` - Новая страница
**Структура:**
1. **Заголовок** с кнопкой "Загрузить остатки"
2. **Фильтры:**
   - Выбор групп товаров (мультиселект или чекбоксы)
   - Поле поиска (клиентская фильтрация)
3. **Таблица остатков:**
   - Колонки: Группа, Товар, Количество, Цена, Сумма
   - Сортировка по клику на заголовки
   - Пагинация (опционально, если данных много)
4. **Итоговая строка:**
   - Общее количество товаров
   - Общая сумма

**Особенности:**
- Адаптивный дизайн (Bootstrap 5)
- Поиск работает на клиенте (JavaScript)
- Фильтрация по группам - на сервере (при загрузке)

#### 2.3. `static/css/styles.css` - Стили для страницы остатков
```css
/* Стили для таблицы остатков */
.stock-table { ... }
.stock-search { ... }
.stock-filters { ... }
.stock-summary { ... }
```

### 3. JavaScript (Минимальный, только для поиска)

#### 3.1. Поиск по таблице
```javascript
function filterStockTable(searchQuery) {
  // Фильтрация строк таблицы по поисковому запросу
  // Поиск по: GROUP_NAME, GOOD_NAME
}
```

---

## 📝 ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ

### Этап 1: Backend - Proxy Client
- [ ] Добавить константу `STOCK_QUERY_TEMPLATE` в `ProxyAPIClient`
- [ ] Реализовать метод `get_stock(group_ids=None)`
- [ ] Добавить опциональную фильтрацию по группам в SQL
- [ ] Протестировать метод с реальными данными

### Этап 2: Backend - Services
- [ ] Создать файл `services/stock.py`
- [ ] Реализовать функции фильтрации и поиска
- [ ] Добавить функцию расчета итогов
- [ ] Написать unit-тесты

### Этап 3: Backend - Routes
- [ ] Добавить маршрут `/stock` в `app.py`
- [ ] Реализовать логику загрузки данных по `load=1`
- [ ] Добавить обработку фильтров по группам
- [ ] Обработка ошибок API

### Этап 4: Frontend - Navigation
- [ ] Добавить пункт "Остатки" в `base.html`
- [ ] Проверить активное состояние меню

### Этап 5: Frontend - Template
- [ ] Создать `templates/stock_table.html`
- [ ] Добавить кнопку "Загрузить остатки"
- [ ] Реализовать форму фильтров (группы товаров)
- [ ] Создать таблицу для отображения данных
- [ ] Добавить поле поиска
- [ ] Добавить итоговую строку

### Этап 6: Frontend - Styling
- [ ] Добавить стили в `static/css/styles.css`
- [ ] Адаптивность для мобильных устройств
- [ ] Стили для поиска и фильтров

### Этап 7: Frontend - JavaScript
- [ ] Реализовать поиск по таблице
- [ ] Добавить обработчики событий
- [ ] Оптимизация производительности поиска

### Этап 8: Testing
- [ ] Unit-тесты для `get_stock()`
- [ ] Unit-тесты для `services/stock.py`
- [ ] Интеграционные тесты для маршрута `/stock`
- [ ] Проверка, что существующий функционал не сломан

### Этап 9: Documentation
- [ ] Обновить README.md с описанием новой страницы
- [ ] Добавить комментарии в код

---

## 🔍 ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

1. **Фильтрация по складам:**
   - Что именно имеется в виду под "складами"?
   - Это группы товаров (GROUP_NAME) или отдельные склады (STORGRP)?
   - Нужна ли фильтрация на уровне SQL или достаточно на уровне приложения?

2. **Производительность:**
   - Сколько товаров ожидается? (В документации упоминается 1,200+ товаров)
   - Нужна ли пагинация или достаточно поиска/фильтрации?
   - Кэширование результатов на сервере?

3. **UI/UX:**
   - Какой формат отображения цен? (₾ или лари)
   - Нужна ли группировка по категориям в таблице?
   - Экспорт в Excel/CSV?

4. **Фильтрация:**
   - Мультиселект для групп или чекбоксы?
   - Сохранение выбранных фильтров в URL?

---

## ✅ BEST PRACTICES

1. **Код:**
   - Следовать существующему стилю кода
   - Type hints для всех функций
   - Docstrings для публичных методов
   - Обработка ошибок с понятными сообщениями

2. **Архитектура:**
   - Разделение ответственности (client → services → routes → templates)
   - Переиспользование существующих компонентов
   - Минимальные изменения в существующем коде

3. **Тестирование:**
   - Unit-тесты для новой функциональности
   - Проверка обратной совместимости
   - Тестирование на реальных данных

4. **Производительность:**
   - Ленивая загрузка данных (только по запросу)
   - Клиентская фильтрация для поиска
   - Оптимизация SQL запросов

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ

1. **Начать с Backend:**
   - Добавить `get_stock()` в `proxy_client.py`
   - Создать `services/stock.py`
   - Добавить маршрут `/stock`

2. **Затем Frontend:**
   - Обновить навигацию
   - Создать шаблон
   - Добавить стили и JavaScript

3. **Финальная проверка:**
   - Тестирование
   - Проверка существующего функционала
   - Документация

---

## 📌 ПРИМЕЧАНИЯ

- **Не ломать существующий функционал** - все изменения должны быть изолированы
- **Минимальный JavaScript** - только для поиска, остальное на сервере
- **Адаптивный дизайн** - должен работать на мобильных устройствах
- **Производительность** - учитывать большой объем данных (1,200+ товаров)

---

**Готов к обсуждению и уточнению деталей перед началом реализации!**

